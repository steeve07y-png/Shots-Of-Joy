{
  "name": "shots-of-joy-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for the Shots of Joy Start-a-Route enquiry form.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "nodemailer": "^6.10.0"
  }
}
